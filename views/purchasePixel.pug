extends base

block header
block Indexcss
block footer
block content

    script.
        let fbcurrency =!{JSON.stringify(businessAccount.facebookCurrency)}
        value= #{businessAccount.facebookValue}
        fbq('track', 'Purchase', {
        value: `${value}.00`,
        currency: fbcurrency
        });
    script.
        fbq('track', 'Lead');
    script.
        fbq('track', 'CompleteRegistration', {
        value: `${value}.00`,
        currency: fbcurrency
        });
    section
        .error_title
            h1 Firing your Pixel... Check Your Facebook Pixel Helper
            h3 Go back After Confirmation